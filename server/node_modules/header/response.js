var util   = require("util");
var Header = require("./index");

function ResponseHeader(value) {
    Header.call(this, value);
}

util.inherits(ResponseHeader, Header);

module.exports = ResponseHeader;
