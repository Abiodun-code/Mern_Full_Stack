var util          = require("util");
var RequestHeader = require("../request");

function OriginHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(OriginHeader, RequestHeader);

OriginHeader.prototype.getKey = function() {
    return "Origin";
};

module.exports = OriginHeader;
