var util          = require("util");
var RequestHeader = require("../request");

function AcceptHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(AcceptHeader, RequestHeader);

AcceptHeader.prototype.getKey = function() {
    return "Accept";
};

module.exports = AcceptHeader;
