var util          = require("util");
var RequestHeader = require("../request");

function HostHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(HostHeader, RequestHeader);

HostHeader.prototype.getKey = function() {
    return "Host";
};

module.exports = HostHeader;
