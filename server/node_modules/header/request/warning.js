var util          = require("util");
var RequestHeader = require("../request");

function WarningHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(WarningHeader, RequestHeader);

WarningHeader.prototype.getKey = function() {
    return "Warning";
};

module.exports = WarningHeader;
