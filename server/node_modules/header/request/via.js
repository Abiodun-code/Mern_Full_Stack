var util          = require("util");
var RequestHeader = require("../request");

function ViaHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(ViaHeader, RequestHeader);

ViaHeader.prototype.getKey = function() {
    return "Via";
};

module.exports = ViaHeader;


<?php
namespace PlasmaConduit\headers\request;
use PlasmaConduit\headers\AbstractHttpRequestHeader;

class ViaHeader extends AbstractHttpRequestHeader {

    public function getKey() {
        return "Via";
    }

}
