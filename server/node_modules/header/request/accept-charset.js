var util          = require("util");
var RequestHeader = require("../request");

function AcceptCharsetHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(AcceptCharsetHeader, RequestHeader);

AcceptCharsetHeader.prototype.getKey = function() {
    return "Accept-Charset";
};

module.exports = AcceptCharsetHeader;
