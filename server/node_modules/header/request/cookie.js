var util          = require("util");
var RequestHeader = require("../request");

function CookieHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(CookieHeader, RequestHeader);

CookieHeader.prototype.getKey = function() {
    return "Cookie";
};

module.exports = CookieHeader;
