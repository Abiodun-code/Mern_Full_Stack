var util          = require("util");
var RequestHeader = require("../request");

function UserAgentHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(UserAgentHeader, RequestHeader);

UserAgentHeader.prototype.getKey = function() {
    return "User-Agent";
};

module.exports = UserAgentHeader;
