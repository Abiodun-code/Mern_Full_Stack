var util          = require("util");
var RequestHeader = require("../request");

function MaxForwardsHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(MaxForwardsHeader, RequestHeader);

MaxForwardsHeader.prototype.getKey = function() {
    return "Max-Forwards";
};

module.exports = MaxForwardsHeader;
