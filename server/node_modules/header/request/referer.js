var util          = require("util");
var RequestHeader = require("../request");

function RefererHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(RefererHeader, RequestHeader);

RefererHeader.prototype.getKey = function() {
    return "Referer";
};

module.exports = RefererHeader;
