var util          = require("util");
var RequestHeader = require("../request");

function RangeHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(RangeHeader, RequestHeader);

RangeHeader.prototype.getKey = function() {
    return "Range";
};

module.exports = RangeHeader;
