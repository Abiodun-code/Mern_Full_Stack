var util          = require("util");
var RequestHeader = require("../request");

function AcceptLanguageHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(AcceptLanguageHeader, RequestHeader);

AcceptLanguageHeader.prototype.getKey = function() {
    return "Accept-Language";
};

module.exports = AcceptLanguageHeader;
