var util          = require("util");
var RequestHeader = require("../request");

function ContentMd5Header(value) {
    RequestHeader.call(this, value);
}

util.inherits(ContentMd5Header, RequestHeader);

ContentMd5Header.prototype.getKey = function() {
    return "Content-MD5";
};

module.exports = ContentMd5Header;
