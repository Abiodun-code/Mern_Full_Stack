var util          = require("util");
var RequestHeader = require("../request");

function IfRangeHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(IfRangeHeader, RequestHeader);

IfRangeHeader.prototype.getKey = function() {
    return "If-Range";
};

module.exports = IfRangeHeader;
