var util          = require("util");
var RequestHeader = require("../request");

function ConnectionHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(ConnectionHeader, RequestHeader);

ConnectionHeader.prototype.getKey = function() {
    return "Connection";
};

module.exports = ConnectionHeader;
