var util          = require("util");
var RequestHeader = require("../request");

function FromHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(FromHeader, RequestHeader);

FromHeader.prototype.getKey = function() {
    return "From";
};

module.exports = FromHeader;
