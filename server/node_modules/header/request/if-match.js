var util          = require("util");
var RequestHeader = require("../request");

function IfMatchHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(IfMatchHeader, RequestHeader);

IfMatchHeader.prototype.getKey = function() {
    return "If-Match";
};

module.exports = IfMatchHeader;
