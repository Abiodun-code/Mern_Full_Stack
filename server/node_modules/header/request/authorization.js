var util          = require("util");
var RequestHeader = require("../request");

function AuthorizationHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(AuthorizationHeader, RequestHeader);

AuthorizationHeader.prototype.getKey = function() {
    return "Authorization";
};

module.exports = AuthorizationHeader;
