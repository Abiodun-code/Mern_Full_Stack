var util          = require("util");
var RequestHeader = require("../request");

function DateHeader(value) {
    RequestHeader.call(this, value);
}

util.inherits(DateHeader, RequestHeader);

DateHeader.prototype.getKey = function() {
    return "Date";
};

module.exports = DateHeader;
