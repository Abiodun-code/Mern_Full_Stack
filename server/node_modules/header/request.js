var util   = require("util");
var Header = require("./index");

function RequestHeader(value) {
    Header.call(this, value);
}

util.inherits(RequestHeader, Header);

module.exports = RequestHeader;
