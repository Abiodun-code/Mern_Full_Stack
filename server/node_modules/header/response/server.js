var util           = require("util");
var ResponseHeader = require("../response");

function ServerHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ServerHeader, ResponseHeader);

ServerHeader.prototype.getKey = function() {
    return "Server";
};

module.exports = ServerHeader;
