var util           = require("util");
var ResponseHeader = require("../response");

function LocationHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(LocationHeader, ResponseHeader);

LocationHeader.prototype.getKey = function() {
    return "Location";
};

module.exports = LocationHeader;
