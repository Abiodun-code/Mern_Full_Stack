var util           = require("util");
var ResponseHeader = require("../response");

function ContentEncodingHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ContentEncodingHeader, ResponseHeader);

ContentEncodingHeader.prototype.getKey = function() {
    return "Content-Encoding";
};

module.exports = ContentEncodingHeader;
