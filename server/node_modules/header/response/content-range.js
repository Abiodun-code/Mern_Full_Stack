var util           = require("util");
var ResponseHeader = require("../response");

function ContentRangeHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ContentRangeHeader, ResponseHeader);

ContentRangeHeader.prototype.getKey = function() {
    return "Content-Range";
};

module.exports = ContentRangeHeader;
