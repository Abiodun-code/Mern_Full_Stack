var util           = require("util");
var ResponseHeader = require("../response");

function ExpiresHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ExpiresHeader, ResponseHeader);

ExpiresHeader.prototype.getKey = function() {
    return "Expires";
};

module.exports = ExpiresHeader;
