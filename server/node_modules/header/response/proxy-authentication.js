var util           = require("util");
var ResponseHeader = require("../response");

function ProxyAuthenticationHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ProxyAuthenticationHeader, ResponseHeader);

ProxyAuthenticationHeader.prototype.getKey = function() {
    return "Proxy-Authentication";
};

module.exports = ProxyAuthenticationHeader;
