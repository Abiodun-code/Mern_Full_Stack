var util           = require("util");
var ResponseHeader = require("../response");

function AgeHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(AgeHeader, ResponseHeader);

AgeHeader.prototype.getKey = function() {
    return "Age";
};

module.exports = AgeHeader;
