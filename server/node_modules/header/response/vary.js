var util           = require("util");
var ResponseHeader = require("../response");

function VaryHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(VaryHeader, ResponseHeader);

VaryHeader.prototype.getKey = function() {
    return "Vary";
};

module.exports = VaryHeader;
