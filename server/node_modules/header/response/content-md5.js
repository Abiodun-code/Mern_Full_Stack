var util           = require("util");
var ResponseHeader = require("../response");

function ContentMd5Header(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ContentMd5Header, ResponseHeader);

ContentMd5Header.prototype.getKey = function() {
    return "Content-MD5";
};

module.exports = ContentMd5Header;
