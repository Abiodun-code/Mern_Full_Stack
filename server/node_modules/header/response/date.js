var util           = require("util");
var ResponseHeader = require("../response");

function DateHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(DateHeader, ResponseHeader);

DateHeader.prototype.getKey = function() {
    return "Date";
};

module.exports = DateHeader;
