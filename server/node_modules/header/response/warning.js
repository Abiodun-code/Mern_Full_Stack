var util           = require("util");
var ResponseHeader = require("../response");

function WarningHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(WarningHeader, ResponseHeader);

WarningHeader.prototype.getKey = function() {
    return "Warning";
};

module.exports = WarningHeader;
