var util           = require("util");
var ResponseHeader = require("../response");

function SetCookieHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(SetCookieHeader, ResponseHeader);

SetCookieHeader.prototype.getKey = function() {
    return "Set-Cookie";
};

module.exports = SetCookieHeader;
