var util           = require("util");
var ResponseHeader = require("../response");

function ConnectionHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(ConnectionHeader, ResponseHeader);

ConnectionHeader.prototype.getKey = function() {
    return "Connection";
};

module.exports = ConnectionHeader;
