var util           = require("util");
var ResponseHeader = require("../response");

function CacheControlHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(CacheControlHeader, ResponseHeader);

CacheControlHeader.prototype.getKey = function() {
    return "Cache-Control";
};

module.exports = CacheControlHeader;
