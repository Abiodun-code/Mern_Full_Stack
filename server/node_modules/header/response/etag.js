var util           = require("util");
var ResponseHeader = require("../response");

function EtagHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(EtagHeader, ResponseHeader);

EtagHeader.prototype.getKey = function() {
    return "ETag";
};

module.exports = EtagHeader;
