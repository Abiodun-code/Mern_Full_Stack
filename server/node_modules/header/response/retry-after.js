var util           = require("util");
var ResponseHeader = require("../response");

function RetryAfterHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(RetryAfterHeader, ResponseHeader);

RetryAfterHeader.prototype.getKey = function() {
    return "Retry-After";
};

module.exports = RetryAfterHeader;
