var util           = require("util");
var ResponseHeader = require("../response");

function AllowHeader(value) {
    ResponseHeader.call(this, value);
}

util.inherits(AllowHeader, ResponseHeader);

AllowHeader.prototype.getKey = function() {
    return "Allow";
};

module.exports = AllowHeader;
